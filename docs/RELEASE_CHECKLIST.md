# Version 1 Release Checklist

Last reviewed: 2026-08-15 (Asia/Bangkok)

Status meanings: `OPEN` still requires work, `BLOCKED` requires an OWNER decision or external access, and `CLOSED` has repeatable evidence that meets the release criterion.

| Order | Release gate | Status | Evidence required to close |
| --- | --- | --- | --- |
| 1 | Data strategy | CLOSED | OWNER selected `CLEAN_SEED`; the decision and clean-production boundaries are recorded in `docs/DECISIONS.md` and `docs/MIGRATION_PLAN.md`. |
| 2 | Staging database and automated integrity | CLOSED | The linked CLEAN staging database was rebuilt from empty and migration history matches through `202608150001`; schema lint, pgTAP, all 7 integration files, and all 39 tests passed. The post-test fixture audit found no Auth users, memberships, customers, pets, booking groups, or receipts. |
| 3 | Backup and restore drill | CLOSED | GitHub Actions restore drill [`31830796846`](https://github.com/hugpaengpetworld/hugpaeng-admin/actions/runs/31830796846) restored commit `a0203d9` from hard-locked CLEAN staging into the isolated restore target, reconciled schema and application data, passed Auth smoke tests and all 39 database integration tests, cleaned fixtures, and removed ephemeral backup files. |
| 4 | Application and Cloudflare build | CLOSED | GitHub Actions quality run [`31831680014`](https://github.com/hugpaengpetworld/hugpaeng-admin/actions/runs/31831680014) passed both application and database jobs for commit `e9b7465`; the application job passed format, lint, strict TypeScript, unit tests, Next.js production build, and OpenNext Cloudflare Worker build. |
| 5 | Staging deployment and smoke tests | CLOSED | Cloudflare Worker staging version `d484e4c0-a73f-4a70-a12b-47827c81619b` passed public booking, Auth, protected pages, room planning, check-in/out, Dynamic PromptPay settlement, immutable receipt print, cron authorization, outbox delivery, and a real scheduled-event check; CLEAN staging was reset and audited afterward. |
| 6 | Production configuration and security | CLOSED | OWNER-approved domain, Supabase Auth redirects, Cloudflare secrets, LINE production webhook, retention and finance boundaries, temporary-support policy, production OWNER authentication, security headers, and Worker observability are configured and evidenced. |
| 7 | Device and clinic acceptance | OPEN | OWNER signs off critical flows on desktop, tablet, and smartphone, including keyboard/accessibility and 80 mm receipt print. |
| 8 | Cutover and rollback | OPEN | Exact release identifier, write-freeze/delta plan when applicable, smoke checklist, rollback owner, and post-write reconciliation procedure are approved. |

## Evidence captured so far

- Gate 1 closed: the OWNER selected `CLEAN_SEED` on 2026-08-14. Production will be a separate clean project built only from reviewed migrations/seed and newly provisioned Auth users.
- The reviewed seed contains only the first tenant, tenant settings, CAT01–CAT11, and DOG01–DOG07. It does not fabricate Auth users or insert customers, pets, bookings, stays, payments, receipts, or audit activity.
- The OWNER explicitly approved `bmp-booking-dev` (`wnnxdcxuxupmnplkegkt`) as destructive CLEAN staging. On 2026-08-14 it was reset from migrations `202608050001`–`202608140005` and `supabase/seed.sql`; local and remote migration histories now match through `202608140005`.
- Linked `public` and `extensions` schema lint reports no errors.
- Local `.env.local` contains Supabase, service-role, application URL, cron, and rate-limit values; it does not contain LINE credentials or `TEST_DATABASE_URL`. Local presence is not evidence that Cloudflare production secrets are configured.
- Cloudflare Wrangler is authenticated to account `dd0c01bdf56fa7bba2e915d0522a9666`. This identifies the approved staging account only; credentials and Worker secrets remain outside source control.
- Latest local checks on 2026-08-14: formatter, lint, strict TypeScript, 64 non-database tests, Next.js production build, and OpenNext Cloudflare Worker build pass. The default test command skips 39 database integration assertions when `TEST_DATABASE_URL` is absent. Both the full and production-only `npm audit` report zero known vulnerabilities after pinning the patched `nanoid` release.
- `npm ci --dry-run --ignore-scripts` accepts the updated lockfile. Before the authorized reset, the linked Supabase `db push --dry-run` reported exactly migrations `202608140002`–`202608140005` pending with no seed or role writes.
- Post-reset staging evidence: schema lint reports no errors; the expanded authenticated run passed all 7 integration files and all 39 tests with no skips. The post-test audit found zero Auth users, tenant memberships, customers, pets, booking groups, receipts, test users, and test platform roles. The seed contains exactly one tenant and its configured room inventory.
- Supabase CLI's pgTAP command still requires Docker on this Windows host even with `--linked`, so it could not run locally. The database integration suite connects directly through the masked Session pooler password prompt and is the available staging verification path.
- `npm run test:integration:staging` accepts only the database password through a masked PowerShell prompt, builds the URI from the linked Session pooler URL with percent-encoding, keeps it process-only, and clears it after the run.
- The 2026-08-14 staging integration attempt reached the Supabase Session pooler but authentication was rejected before fixture setup (`password authentication failed`); all 34 assertions were skipped and no test fixture was created.
- The authenticated staging rerun passed 33 of 34 assertions and exposed a real room-plan check-in replay bug plus a test-only platform-role cleanup ordering issue. Migration `202608140001` and regression coverage were applied; a final authenticated rerun is pending.
- The post-migration rerun verified the idempotency and cleanup fixes. It passed 33 of 34 tests; the remaining workflow crossed Vitest's default five-second limit by 14 ms on Supabase Cloud without an assertion or database error. The integration-only timeout is now 20 seconds, with a 30-second hook timeout, pending one final rerun.
- The final authenticated Supabase staging run passed all 6 integration files and all 34 tests with no skips.
- A service-role read-only audit found zero Auth users ending in `@example.invalid` and zero associated platform roles. No stale integration fixture requires deletion.
- Gate 2 closed on 2026-08-14: GitHub Actions Linux quality run [`31786774396`](https://github.com/hugpaengpetworld/hugpaeng-admin/actions/runs/31786774396) passed both jobs. The database job started local Supabase, reset from an empty database, ran database lint, pgTAP, and all 34 integration tests successfully. The application job also passed formatter, lint, strict TypeScript, unit tests, Next.js production build, and OpenNext Cloudflare Worker build for commit `53b4d9f`.
- OpenNext Cloudflare build passed in the Windows workspace and previously passed in Linux CI for commit `53b4d9f`. After Gate 3 closed, the complete application checks were repeated against the current release source.
- Gate 2 was reopened after approval of the six-role capability model and central patient registry, then closed again on 2026-08-14 after the authorized CLEAN staging rebuild, expanded 39-test integration run, and zero-fixture audit all passed.
- Gate 3 preparation on 2026-08-14 confirmed that `supabase db dump --linked` can authenticate to CLEAN staging but requires Docker to run `pg_dump` on this Windows host. Docker and native PostgreSQL client tools are unavailable, no valid dump was produced, and the three zero-byte output files were removed. The restore drill must use an isolated Supabase target and either a Linux CI runner with protected database secrets or approved PostgreSQL client tooling; it must never restore over the verified CLEAN staging source.
- The OWNER created isolated Supabase restore target `bmp-booking-restore-drill` (`svgmzjphmdqfeptalxhe`) in Singapore. The manual Linux workflow is hard-locked to that target and source `wnnxdcxuxupmnplkegkt`; protected passwords and API keys are stored only as GitHub Actions repository secrets.
- Security regression coverage identified that trigger function `public.assign_patient_hn()` was executable by exposed roles. Migration `202608150001` revoked that privilege from `public`, `anon`, and `authenticated`; GitHub Actions quality run [`31830248965`](https://github.com/hugpaengpetworld/hugpaeng-admin/actions/runs/31830248965) then passed both application and database jobs for commit `a0203d9`, and the migration was applied to CLEAN staging with matching local/remote history.
- Gate 3 closed on 2026-08-15: restore drill [`31830796846`](https://github.com/hugpaengpetworld/hugpaeng-admin/actions/runs/31830796846) completed successfully for commit `a0203d9` in 7 minutes 42 seconds. The workflow created checksum evidence in the ephemeral runner without uploading database artifacts, reset only the hard-locked isolated target, restored the `public` application schema and data, reconciled every public table and financial totals, passed Auth create/login/delete smoke tests, passed all 7 integration files and all 39 tests, cleaned test fixtures, and securely removed temporary backup files. The GitHub Node.js 20 action-runtime deprecation annotation is non-blocking workflow-maintenance debt; it did not skip or fail any release assertion.
- Gate 4 closed on 2026-08-15: GitHub Actions quality run [`31831680014`](https://github.com/hugpaengpetworld/hugpaeng-admin/actions/runs/31831680014) completed successfully for commit `e9b7465`. Its application job passed Prettier format checking, ESLint, strict TypeScript checking, unit tests, the Next.js production build, and the OpenNext Cloudflare Worker build. Its database job also completed successfully, leaving the full workflow green before the release process advances to staging deployment.
- Gate 5 closed on 2026-08-15: OpenNext deployed Cloudflare Worker `bmp-booking-staging` version `d484e4c0-a73f-4a70-a12b-47827c81619b` to `https://bmp-booking-staging.hugpaeng-petworld.workers.dev`, hard-locked to CLEAN Supabase staging project `wnnxdcxuxupmnplkegkt`. The end-to-end smoke exercised the public home, availability, booking request/status, Supabase Auth login, protected admin pages, booking approval, room planning, check-in, checkout, Dynamic PromptPay settlement, immutable receipt print, cron authorization, and outbox completion. A separate scheduled-event probe confirmed the one-minute Cloudflare trigger claimed and completed exactly one synthetic outbox event. The authorized linked reset then rebuilt staging from reviewed migrations and seed; the final audit found zero Auth users, memberships, customers, pets, booking groups, receipts, test users, and test platform roles, with exactly the one seeded tenant remaining.
- Gate 6 started on 2026-08-15 against the dedicated Supabase production project `bmp-booking-production` (`dghipgebiioxphbbyvxp`). The project was linked explicitly, all reviewed migrations through `202608150001` were applied, and `supabase/seed.sql` created only the approved CLEAN_SEED tenant/settings/room inventory. Production helper scripts and Wrangler configuration are hard-locked to this project, Cloudflare account `dd0c01bdf56fa7bba2e915d0522a9666`, Worker `bmp-booking-production`, and `https://admin.hug-paeng.com`.
- The post-seed production service-role audit was read-only and found exactly one tenant plus zero Auth users, tenant memberships, customers, pets, booking groups, receipts, test users, and platform roles. Local and remote migration histories match through `202608150001`; a linked dry run reports no pending migrations, seeds, or role changes. Production Supabase and LINE credentials exist only in Git-ignored `.env.production.local` and encrypted Cloudflare Worker secrets; their values were not printed or committed.
- The OWNER configured Supabase Auth Site URL `https://admin.hug-paeng.com` and redirect URL `https://admin.hug-paeng.com/auth/callback`. After the obsolete `admin.hug-paeng.com` GitHub Pages CNAME was removed without altering `hotel`, `member`, or `www`, OpenNext deployed production Worker version `75c4b1bb-097e-463d-9408-15b901309e1d` with all eight encrypted secrets, the custom domain, the one-minute cron trigger, and the reviewed production security headers.
- Read-only production smoke checks returned `200` for `/` and `/admin/login`, redirected an anonymous `/admin` request to `/admin/login?reason=session`, rejected an unsigned cron POST with `401 UNAUTHORIZED`, and rejected an unsigned LINE webhook POST with `401 INVALID_SIGNATURE`. A direct response-header check confirmed HSTS, CSP, `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, strict-origin referrer policy, and restrictive permissions policy. The OWNER then verified `https://admin.hug-paeng.com/api/line/webhook` successfully in LINE Developers and enabled `Use webhook` on 2026-08-15.
- After a separate explicit authorization, the production bootstrap sent an invitation to `admin@hug-paeng.com`, created its first-tenant `OWNER` membership, and recorded the bootstrap audit event without printing credentials. The invited OWNER must complete the newest email link and verify an authenticated production session before Gate 6 closes.
- The invited OWNER completed the production password flow and confirmed a successful authenticated session at `https://admin.hug-paeng.com` on 2026-08-15. A separate read-only Wrangler status check confirmed 100% of production traffic on Worker version `75c4b1bb-097e-463d-9408-15b901309e1d`; Worker observability is enabled, preview URLs and `workers.dev` are disabled for production, and the custom domain plus one-minute scheduled trigger remain active.
- Gate 6 closed on 2026-08-15 after the OWNER approved the production support policy: support is off by default, no production support account exists, each incident requires explicit OWNER approval plus reason/ticket and minimum read-only scopes, the operating window is at most two hours (with a 24-hour database hard maximum), every use is audited, and grants can be revoked immediately. Support cannot write tenant data, mutate finance/refunds, manage users or roles, access secrets, or browse tenants without a scoped grant.
- Gate 7 device review found the mobile HOME navigation panel aligned to the right edge of a left-side trigger, which pushed most of the panel off-screen and covered the hero text. The panel now opens from the trigger's left edge, is capped at 340 px, and never exceeds the viewport minus 32 px. Formatter, lint, strict TypeScript, all 64 non-database tests, Next.js production build, and OpenNext Cloudflare build passed; the fix is deployed as production Worker version `c9a64486-0199-4e76-b173-6551d00cf8e9`. Final OWNER device sign-off remains open.
- Gate 7 non-mutating browser checks passed on 2026-08-15 for representative smartphone `390 × 844`, tablet `820 × 1180`, and desktop `1440 × 900` viewports: the public portal and admin login had no horizontal overflow, primary mobile controls were approximately 44–48 px high, and login keyboard order/focus indicators were visible. Anonymous access to all reviewed admin routes redirected to `/admin/login?reason=session`. The remaining physical-device, operational-flow, and 80 mm printer checks are tracked in `docs/GATE7_ACCEPTANCE.md` and still require OWNER sign-off.
- Gate 7.1 passed on 2026-08-15: the OWNER signed off the production desktop navigation check after logging in and opening HOME, bookings, cat/dog rooms, sterilization, finance, and settings without reporting horizontal overflow, overlapping content, or dialog problems. Tablet, smartphone operational flows, keyboard/modal behavior, and the physical 80 mm receipt print remain open.
- Gate 7.2 passed on 2026-08-15: the OWNER signed off the production tablet check in portrait and landscape, including navigation, room/calendar cards, and opening, scrolling, and closing dialogs without reporting overflow or overlapping content. Smartphone operational flows, keyboard/modal behavior, and the physical 80 mm receipt print remain open.
- Gate 7.3 passed on 2026-08-15: the OWNER signed off the production smartphone check for the HOME menu, module navigation, room/booking cards, dialog scrolling and closing, touch controls, readable content, and absence of horizontal overflow. Physical-device coverage is complete; keyboard/modal accessibility and the physical 80 mm receipt print remain open.

## Owner decisions still required

1. Production cutover date.
2. Restore objectives and final clinic acceptance.
